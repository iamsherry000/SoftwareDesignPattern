package TemplateMethod;

import TemplateMethod.common.Group;
import TemplateMethod.common.GroupingStrategy;
import TemplateMethod.common.Student;
//import TemplateMethod.common.ReadStudents;

import java.io.IOException;
import java.util.List;

public class Main {
    public static void main(String[] args) throws IOException {
        /*
        GroupingStrategy groupingStrategy = new TimeSlotsBasedGroupingStrategy();

        List<Student> students = ReadStudents.fromFile("student.data");
        List<Group> groups = groupingStrategy.group(students);

        printGroups(groups);
        */
    }
}